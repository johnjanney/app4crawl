# Open Questions

Questions that arise during development and require decisions before proceeding.
Log here rather than making silent assumptions.

| # | Question | Context | Status |
|---|----------|---------|--------|
| 1 | Initial scaffold version tag: `v0.0.1` vs `v0.1.0`? | §12 Phase 1 says tag the initial commit `v0.0.1`, while §10 (CHANGELOG template) and §11 (tagging example) reference `v0.1.0`/`0.1.0`. Followed §12 literally: tagged `v0.0.1` and added a matching `[0.0.1]` CHANGELOG entry. | Resolved (using v0.0.1 per §12) |
| 2 | What software license should the project ship under? | Resolved: MIT (owner decision). `LICENSE` added; README updated. | Resolved (MIT) |
| 3 | Xcode project was hand-authored (no macOS/Xcode in the build environment). | The CI/dev container is Linux with no Swift toolchain. The `.xcodeproj` and Swift sources were written by hand to be valid and openable in Xcode 15+. They should be opened/verified on a real Mac before relying on the build. | Open (needs verification on macOS) |
| 4 | Install flow implemented natively in Swift rather than shelling out to `scripts/setup_env.sh`. | §4 lists shell commands and §9 notes the scripts are "used by the onboarding flow." Implemented the steps natively in `EnvironmentChecker.install()` for reliable per-step live streaming and to avoid bundling/sandbox-PATH issues. `scripts/setup_env.sh` and `check_env.sh` are retained as equivalent CLI/dev tooling. | Resolved (native impl; scripts retained) |
| 7 | Install flow also installs `fastapi` + `uvicorn[standard]` into the managed venv. | §4's commands install only `crawl4ai`, but §2's architecture runs the bundled FastAPI server from that same venv — so it also needs fastapi/uvicorn. Added a "server dependencies" install step. (`setup_env.sh` mirrors §4 only; consider aligning it or installing from `server/requirements.txt` when packaging.) | Resolved (added step) |
| 8 | Result viewer uses a lightweight built-in Markdown renderer (headings, lists, code, inline) rather than a third-party library. | Keeps v1 dependency-free; the wire contract was verified against live server JSON. A richer renderer (e.g. swift-markdown-ui) can be swapped into `MarkdownView` later without changing call sites. | Resolved (built-in; swappable) |
| 9 | Crawl progress uses status polling (0.5s); SSE client exists but is unused by the UI. | Polling is simpler/robust for v1. `CrawlAPIClient.events(jobID:)` provides the SSE stream for live per-page updates when the UI adopts it. | Open (SSE adoption optional) |
| 10 | Custom LLM provider mapping to Crawl4AI/LiteLLM is incomplete. | `routers/crawl.py` builds the provider string as `provider/model`, which yields `custom/<model>` for the custom provider — not a valid LiteLLM target. The UI/Keychain/base-URL plumbing is done; the server should map "custom" to a LiteLLM `openai/`-compatible provider using the supplied base URL. Cloud providers (OpenAI/Anthropic/Gemini/Ollama) are unaffected. | Open (server-side, custom only) |
| 11 | Crawl history stores full page results (including raw HTML) on disk. | Lets users reopen past results without re-crawling. Bounded to the 100 most recent records; large HTML could still grow the file. Consider storing summaries + lazy result files, or excluding raw HTML, if size becomes an issue. | Open (acceptable for v1) |
| 12 | Promote `v1.0.0-rc.1` → `v1.0.0`. | Resolved: the app builds and runs on macOS (crawl, extraction, export, history all verified by the owner). Version bumped to `1.0.0` in `CHANGELOG`, server `APP_VERSION`, and Xcode `MARKETING_VERSION`; tagged `v1.0.0`. (Code signing + notarization still required before distributing to other users.) | Resolved (v1.0.0) |
| 5 | Added `Services/AppPaths.swift` and `Services/ProcessRunner.swift` (not in the §9 file list). | §13 mandates "use constants files" (AppPaths) and the install/detection flows need a shared streaming subprocess helper (ProcessRunner). Both placed under the existing `Services` group. | Resolved (supporting utilities) |
| 6 | Bundling of `server/` into the app. | Resolved: `server/` is added to the app target as a folder reference in Copy Bundle Resources, so it ships inside `App4Crawl.app/Contents/Resources/server` and `ServerManager.bundledServerDirectory()` resolves at runtime. (The active-environment status indicator is shown in onboarding and as a toolbar dot in the main window.) | Resolved |
